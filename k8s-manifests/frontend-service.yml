apiVersion: v1 
kind: Service
metadata:
  name: frontend-service
  labels:
    app: smallboy
    type: frontend
spec:
 type: ClusterIP
 ports:
  - targetPort: 3000
    port: 80
 selector:
  app: smallboy
  type: frontend