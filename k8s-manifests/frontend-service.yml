apiVersion: v1 
kind: Service
metadata:
  name: frontend-service
  namespace: sb-ns
  labels:
    app: smallboy
    type: frontend
spec:
 type: ClusterIP
 ports:
  - name: http
    targetPort: 80
    port: 3000
 selector:
  app: smallboy
  type: frontend