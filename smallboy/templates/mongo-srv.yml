apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}mongo-svc
  namespace: sb-ns
spec:
  selector:
    {{- include "datblabel" . | indent 2  }}
  ports:
    - port: {{ .Values.database.service.port }}
      targetPort: {{ .Values.database.service.port }}
